
  # Bill Splitter App

  This is a code bundle for Bill Splitter App. The original project is available at https://www.figma.com/design/EVbtRg4vhZTHR27aqNQZRe/Bill-Splitter-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  